/* let num1 = parseInt(prompt('Número: '))

while(isNaN(num1)) {
    num1 = parseInt(prompt('Ingresa un número: '))
}
 */

/* let num1 = parseInt(prompt('Número: '))
let secretNumber = 23;
let intento = 0

while(num1 !== secretNumber) {
    console.log("No has adivinado el número secreto")
    num1 = parseInt(prompt('Número: '));
    intento++;
    console.log(`Intento ${intento}`)
}

console.log("Has adivinado el número secreto.") */

// let num1 = parseInt(prompt('Número: '))

// do {
//     console.log("El número es válido")
//     num1 = parseInt(prompt('Número: '))
// } while( num1 <= 5)

/* var settings = {
    "url": "https://api.countrylayer.com/v2/all? access_key = (9e0c63f15447cd0fad5a32ccf5c84bb3)",
    "method": "GET",
    "timeout": 1000,
    };
    
    $.ajax(settings).done((response) => {
        console.log (response);
    }); */